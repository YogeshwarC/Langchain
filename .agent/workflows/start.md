---
description: Runs the AI Tutor backend server.
---

1. RUN SERVER:
   Run command: `python -m interfaces.api_server`

2. INSTRUCTIONS:
   Once the server start log appears (Uvicorn running on http://0.0.0.0:8000), open your browser to `http://localhost:8000`.
